// Configuration for API endpoints
// Replace these URLs with your backend endpoints when merging

const API_BASE_URL = 'https://api.example.com'; // Replace with actual base URL

export const API_ENDPOINTS = {
  CONVERT_AUDIO: `${API_BASE_URL}/convert-audio`,
  ANALYZE_SYMPTOMS: `${API_BASE_URL}/analyze-symptoms`,
  
  // Google Places API key - replace with your own key
  GOOGLE_PLACES_API_KEY: 'YOUR_GOOGLE_PLACES_API_KEY',
};

export default API_ENDPOINTS;
